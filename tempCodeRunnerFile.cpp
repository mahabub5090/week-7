        // for (int i = 0; i < n - 1; i++)
        // {
        //     i % 2 == 0 ? ans.push_back(v1.back()) : ans.push_back(v2.back());
        //     v1.pop_back();
        //     v2.pop_back();
        // }